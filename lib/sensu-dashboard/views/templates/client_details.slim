h3 Client Data
== '<% _.each(_.pairs(this.options.clientModel.attributes), function(i) { %>'
.well.well-small
  h6 <%= i[0] %>
  == '<% if (_.isArray(i[1])) { %>'
  ul
    == '<% _.each(i[1], function(j) { %>'
    li
      == '<%= j %>'
    == '<% }); %>'
  == '<% } else { %>'
  == '<%= i[1] %>'
  == '<% } %>'
== '<% }); %>'
